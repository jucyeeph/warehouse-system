# NAS 测试端离线快速构建镜像
# 测试 NAS 会保留上一次成功构建的测试镜像，其中已有 Node 依赖、
# better-sqlite3 和 sharp。新版本以它为基础覆盖应用代码，避免依赖外网镜像仓库。

FROM ddq-warehouse-system-test-warehouse-test:latest

WORKDIR /app

COPY server.js thumbnail-service.js thumbnails-cli.js ./

EXPOSE 3000
CMD ["node", "server.js"]
